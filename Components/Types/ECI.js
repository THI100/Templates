export default function ECItem (id, name, description, price, category, isAvaliable, createdAt, updated, updatedAt) {
    return {
        id,
        name,
        description,
        price,
        category,
        isAvaliable,
        createdAt,
        updated,
        updatedAt
    }
};